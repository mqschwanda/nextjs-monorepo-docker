import { Typography } from '@mqs/react-server-components';

export const metadata = {
  title: 'About',
};

export default function Page() {
  return (
    <div>
      <Typography>
        About
      </Typography>
    </div>
  );
}
