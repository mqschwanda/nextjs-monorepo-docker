import { Typography } from '@mqs/react-server-components';

export const metadata = {
  title: 'Home',
};

export default function Page() {
  return (
    <div>
      <Typography
        variant='h4'
      >
        Home
      </Typography>
    </div>
  );
}
