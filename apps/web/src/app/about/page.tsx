import { Typography } from '@mqs/react-server-components';

export const metadata = {
  title: 'About',
};

export default function Page() {
  return (
    <div>
      <Typography
        variant='h4'
      >
        About
      </Typography>
    </div>
  );
}
